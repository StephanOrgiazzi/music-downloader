{
  "name": "music-downloader-skill",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "use": "npm --prefix ./scripts install && node ./scripts/run.mjs doctor",
    "doctor": "node ./scripts/run.mjs doctor",
    "build": "npm --prefix ./scripts run build"
  }
}
