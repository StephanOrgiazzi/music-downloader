{
  "name": "music-downloader-skill",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "use": "npm --prefix ./scripts install && node ./run.mjs doctor",
    "doctor": "node ./run.mjs doctor",
    "build": "npm --prefix ./scripts run build"
  }
}
